# Upstream backend that the Next server proxies `/api/*` to (server-only).
API_PROXY_TARGET=https://asantemeats.ca/api
# What the browser calls. Keep this same-origin so the refresh cookie works.
NEXT_PUBLIC_API_BASE=/api
