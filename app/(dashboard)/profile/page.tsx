import { type Metadata } from "next"

export const metadata: Metadata = {
  title: "Profile",
}

export default function ProfilePage() {
  return (
    <div>
      <h2>Profile page</h2>
    </div>
  )
}
